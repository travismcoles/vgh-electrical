services:
  - type: web
    name: vgh-electrical
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      # Optional: attach a Render PostgreSQL and set DATABASE_URL to its internal URL
